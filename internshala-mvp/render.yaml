services:
  - type: static
    name: internshala-mvp-frontend
    buildCommand: npm run build
    publishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
